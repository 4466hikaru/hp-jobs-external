# job-013: 自分のバランス監査(job-011)で見つけた明確バグの修正パッチ

- status: open
- 難度: 中(content JSON+小規模GDScript)
- 納品方法: A(branch `job/013` → PR)

## 対象(判断不要の明確バグのみ。数値バランスの変更はオーナー決裁事項なのでやらない)
1. `player.damageReduction` がcap表に無くfallback 0.5に落ちる穴 → cap表への追加(値は現行fallbackと同じ0.5で入れ、変更はしない=挙動不変のまま穴だけ塞ぐ)
2. job-011で「参照先のないID」「絶対に発動しない条件」と判定した死にデータの修正または削除提案(削除は提案リストのみ、実削除しない)
3. carryover基礎率が7ダンジョン中5件fallbackに落ちている件(job-009 A-02) → fallbackでなく明示値を持つようJSONを補完(値は現行実効値と同一=挙動不変)

## ルール
- **挙動を変えない修正だけ**(数値チューニング禁止)。各修正に「修正前後で実効値が同一」の検証を付ける
- 改行LF/削除最小/適用後コンパイル確認(020事故の教訓: apply成功≠安全)
- ミラーが古い場合、JSONは現物を読み直して現行構造に合わせる

## 完成定義(Yes/No)
- [ ] 3対象のパッチ+実効値不変の検証ログ
- [ ] 死にデータの削除提案リスト(実削除なし)
- [ ] 正直な申告

## 納品物
`deliverables/job-013/`
